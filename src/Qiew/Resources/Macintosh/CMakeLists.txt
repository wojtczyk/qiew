################################################################################
# CMakeLists.txt
#
# Created on: Jan 25, 2011
# Copyright Martin Wojtczyk <martin.wojtczyk@gmail.com>
################################################################################
project(Macintosh)

#set(CPACK_GENERATOR "PackageMaker")

#set(CPACK_PACKAGE_FILE_NAME ${PACKAGE_NAME})
#set(CPACK_PACKAGE_ICON "Qiew.icns")
#set(CPACK_BUNDLE_NAME ${PACKAGE_NAME})
#set(CPACK_BUNDLE_ICON "Qiew.icns")
#set(CPACK_BUNDLE_PLIST xx)
#set(CPACK_BUNDLE_STARTUP_COMMAND xx)

#set(CPACK_PREFLIGHT_SCRIPT xx)
#set(CPACK_POSTFLIGHT_SCRIPT xx)
#set(CPACK_POSTUPGRADE_SCRIPT xx)
#set(CPACK_OSX_PACKAGE_VERSION 10.6)

#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_INFO_STRING ${PACKAGE_NAME})
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_ICON_FILE "Qiew.icns")
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_GUI_IDENTIFIER org.${PACKAGE_NAME})
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_LONG_VERSION_STRING ${PACKAGE_VERSION})
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_BUNDLE_NAME ${PACKAGE_NAME})
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_SHORT_VERSION_STRING ${PACKAGE_VERSION})
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_BUNDLE_VERSION ${PACKAGE_VERSION})
#set_target_properties(${PROJECT_ROOT_NAME} PROPERTIES MACOSX_BUNDLE_COPYRIGHT ${PACKAGE_BUGREPORT})
